# PostgreSQL（Phase 2/3: Workflow Events + Human Approval）
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=hotel_ai

# Redis（Phase 2/3: Real-time Pub/Sub）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Phase 3: Human Approval（可选）
# 审批超时时间（秒），超时后自动提醒
APPROVAL_TIMEOUT_SECONDS=3600
