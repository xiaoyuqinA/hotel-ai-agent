{
  "name": "chrome-extension",
  "version": "1.0.0",
  "description": "智能生成酒店评论回复的 Chrome 扩展。",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp public/manifest.json dist/ && mkdir -p dist/icons && cp -R public/icons/* dist/icons/ && mkdir -p dist/popup && cp src/popup/index.html dist/popup/ && cp src/content-script/styles.css dist/",
    "preview": "vite preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
